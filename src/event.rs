/*
enum Event {
    ScrollUp,
    ScrollRight,
    ScrollDown,
    ScrollLeft
}


pub struct EventManager {
    events: Vec<Event>
}


impl EventManager {
    fn register(self, event: Event) {
        self.events.push(event);
    }
}


trait EventManagerTrait {
    fn process(event: Event);
}


impl Iterator for EventManager {
    type Item = Event;
    
    fn next(&mut self) -> Option<Event> {
        return self.events.iter().next();
    }
}
*/
